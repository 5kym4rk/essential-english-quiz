const fs=require('fs'),vm=require('vm');
const setup=fs.readFileSync('test-quiz.cjs','utf8').split('vm.createContext(ctx);')[0].replace("fs.readFileSync('index.html','utf8')","fs.readFileSync('technical.html','utf8')").replace("fs.readFileSync('data.json','utf8')","fs.readFileSync('technical.json','utf8')");
vm.runInNewContext(setup+"\nnodes['term-language'].value='en';nodes.mode.options=[{},{}];\nvm.createContext(ctx);vm.runInContext(fs.readFileSync('config-technical.js','utf8'),ctx);vm.runInContext(source,ctx);\nassert.equal(ctx.words.length,4675);assert.equal(new Set(data.map(w=>w.id)).size,4675);\nassert.equal(ctx.lessonCatalog().length,270);assert.equal(Object.keys(ctx.config.bookNames).length,14);\nassert.equal(ctx.statsKey,'wordcraft-technical-v1');\nconst groups=new Map();for(const w of data){assert(w.english&&w.chinese&&w.meaning&&w.pinyin);const k=w.book+'-'+w.unit;if(!groups.has(k))groups.set(k,[]);groups.get(k).push(w);}\nfor(const group of groups.values()){assert(group.length<=20);assert.equal(new Set(group.map(w=>w.subdomain)).size,1);}\nlet utterance;ctx.window.SpeechSynthesisUtterance=function(t){this.text=t;};ctx.window.speechSynthesis={speak(u){utterance=u;},cancel(){}};\nfor(const lang of ['en','zh']){\n nodes['term-language'].value=lang;nodes['term-language'].onchange();\n nodes.activity.value='learn';nodes.book.value='1';nodes.unit.value='1';ctx.begin();\n const first=ctx.queue[0],ids=ctx.queue.map(w=>w.id).join();\n assert.equal(nodes.question.textContent,lang==='zh'?first.chinese:first.english);\n assert.equal(nodes['learn-meaning'].textContent,first.meaning);\n assert(nodes['learn-example'].textContent.includes(lang==='zh'?first.english:first.chinese));\n assert.equal(nodes.ipa.textContent,lang==='zh'?first.pinyin:'');\n assert.equal(nodes['toggle-images'].hidden,true);nodes.audio.onclick();assert.equal(utterance.lang,lang==='zh'?'zh-CN':'en-US');\n for(const direction of ['en','vi']){\n  nodes.activity.value='quiz';nodes.mode.value=direction;ctx.begin();\n  assert.equal(ctx.queue.map(w=>w.id).sort().join(),ids.split(',').sort().join());\n  assert.equal(ctx.choices.length,4);assert(ctx.choices.every(w=>w.book===1));\n  assert.equal(nodes.audio.hidden,direction==='vi');\n  ctx.choose(0);assert(nodes.feedback.children.length>0);\n  assert(nodes.feedback.children[2].textContent.includes(lang==='zh'?ctx.queue[0].english:ctx.queue[0].chinese));\n }\n}\nconst storage={'wordcraft-study-stats-v1':'untouched'};ctx.localStorage.getItem=k=>storage[k]||null;ctx.localStorage.setItem=(k,v)=>storage[k]=v;\nnodes.activity.value='learn';ctx.begin();for(let i=0,n=ctx.queue.length;i<n;i++)ctx.advance();\nassert.equal(ctx.studyStats.lessons['1-1'].readingSessions,1);assert.equal(storage['wordcraft-study-stats-v1'],'untouched');\nassert(storage['wordcraft-technical-v1']);\nfor(const page of ['index.html','chinese-vocabulary.html','chinese-grammar.html','chinese-radicals.html','hsk-reference.html','technical.html'])assert(fs.readFileSync(page,'utf8').includes('href=\"technical.html\"'));\nassert(!html.includes('LearnLangLab'));assert(!html.includes('FULL'));\nconsole.log('PASS: 4675 notes, 14 domains, 270 lessons, both languages/quiz directions, speech, no media, isolated statistics and navigation.');\n",{require,console});
